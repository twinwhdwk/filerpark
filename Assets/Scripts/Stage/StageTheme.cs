// Docs/Stages/README.md의 4개 테마. 맵/스테이지 UI가 테마별로 다른 태그 색을 보여줄 때
// 이 값을 기준으로 UITheme의 색을 고른다 (UI 쪽 매핑은 StageIntroUI 참고).
public enum StageTheme
{
    SimultaneousSwitches, // 동시 입력
    StackAndPush,         // 스택 & 푸시
    CarryAndBalance,      // 운반 & 균형
    SharedSurvival,       // 공동 생존
}
